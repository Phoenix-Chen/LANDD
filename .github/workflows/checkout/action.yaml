name: "Checkout Repo"
description: "Checkout Repo"
runs:
  using: "composite"
  steps:
    - name: checkout repo
      uses: actions/checkout@v4
    - name: use node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18.3.1'
    - run: npm install